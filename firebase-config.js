/* PMP Prep — Firebase config
 *
 * REPLACE the placeholder values below with the config you copy from the
 * Firebase Console (Project settings → Your apps → SDK setup and configuration → Config).
 *
 * It is SAFE to commit this file to a public repo. Firebase web API keys are
 * not secrets — security comes from Firestore Security Rules + Auth, which we
 * configure in the Console. See: https://firebase.google.com/docs/projects/api-keys
 *
 * If you leave the placeholder values, the app runs in fully-local mode
 * (no sign-in button, no cloud sync). Manual export/import still works.
 */

window.PMP_FIREBASE_CONFIG = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME"
};
